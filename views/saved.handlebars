<nav class="navbar navbar-default navbar-fixed-top">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="">Saved Articles</a>
                </div>
            
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li ><a href="/">Home</a></li>
                    <li class="active"><a href="/saved">Saved Articles<span class="sr-only">(current)</span></a></li>
                </ul>
                </div><!-- /.navbar-collapse -->
            </div><!-- /.container-fluid -->
        </nav>
        <div class="jumbotron text-center">
            <div class="background-image">

            </div>
            <div class="header-title text-center">
                <h1>Mongo Scraper</h1>
                <p>New York Times Edition</p>
            </div>    
        </div><!--jumbotron--> 

        <!--Saved Articles div-->
        <div class="saved-articles">
            {{#each savedArticles}}
                <div class="panel panel-danger">
                    <div class="panel-heading">
                        <input type="hidden" data-id={{this._id}}>
                        <a href={{this.link}}>{{this.title}}</a>
                        <button type="button" class="btn btn-danger pull-right" id="btn-article-delete">Delete Article</button>
                        <button type="button" class="btn btn-success pull-right" id="btn-article-note" data-toggle="modal" data-target="#noteModal" >Article Notes</button>
                    </div>
                    <div class="panel-body">{{this.summary}}</div>
                </div>
            {{/each}}

        </div> <!-- /.saved-articles -->  
 

  <!-- Modal -->
  <div class="modal fade" id="noteModal" role="dialog">
    <div class="modal-dialog">
    
      <!-- Modal content-->
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal">&times;</button>
          <h4 class="modal-title">Article Notes</h4>
        </div>
        <div class="modal-body">
            <input type="hidden" id="article-id">
            <div id="article-notes">
                <div id="note">
                    
                </div>
            </div>
            <input type="text" id="new-note" placeholder="New Note">
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-success" data-dismiss="modal" id="btn-save-note">Save Note</button>
        </div>
      </div>
      
    </div>
  </div>
  
